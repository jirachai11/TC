import 'package:flutter/material.dart';

class WebsiteHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ความเป็นมาเว็ปไซต์', style: TextStyle(fontSize: 24)),
    );
  }
}